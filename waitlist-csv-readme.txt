Waitlist (Netlify Forms)
========================

Submissions go to Netlify only — nothing is stored in this Git repo.

After deploy: Site configuration → Forms → open the “waitlist” form.

View entries, optional email/Slack notifications, and download submissions as CSV
from the Netlify dashboard.
